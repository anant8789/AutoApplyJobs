pip install pandas
pip install selenium
pip install numpy
pip install pyautogui
pip install pyyaml
pip install selenium webdriver-manager pyyaml
pip install google-generativeai

pip install pandas pyautogui pyyaml beautifulsoup4 selenium webdriver-manager

